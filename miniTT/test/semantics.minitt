id : ∀ a : U . a → a
   = λ a . λ x . x
;

bool : U 
     = Sum (True | False)
;

true : bool
     = True
;

false : bool
      = False
;

elimBool : ∀ c : bool → U . c false → c true → ∀ b : bool . c b
         = λ c . λ h0 . λ h1 . fun (True → h1 | False → h0)
; 